<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/css/single.css">
</head>

<body>
<%- include('../includes/navigation.ejs') %>

<main>
    <% if (singleList) { %>
        <div class="grid">
            <article class="card product-item">
                <header class="card__header">
                    <h1 class="product__title"><%= singleList.name %></h1>
                </header>
                <div class="card__image">
                    <img src="/list/<%= singleList.id %>"
                         alt="<%= singleList.id %>">
                </div>
                <div class="card__content">
                    <h2 class="product__price">$<%= singleList.name %></h2>
                    <p class="product__description"><%= singleList.name %></p>
                </div>
                <div class="card__actions">
                    <a href="/products/<%= singleList.id %>" class="btn">Details</a>
                </div>
            </article>
        </div>
    <% } else { %>
        <h1>No Products Found!</h1>
    <% } %>
</main>
